name: Deploy vue app
on:
  push:
    branches:
      - main  # Replace with the branch you want to use for deployment

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      # Checkout code
      - name: Checkout code
        uses: actions/checkout@v3

      # Clear npm cache
      - name: Clear npm cache
        run: npm cache clean --force

      # Set npm registry to default
      - name: Set npm registry to default
        run: npm config set registry https://registry.npmjs.org/

      # Remove any npm authentication (if any)
      - name: Remove npm authentication (if any)
        run: npm logout || true

      # Set up Node.js
      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      # Install dependencies
      - name: Install dependencies
        run: npm install

      # Build the project
      - name: Build project
        run: npm run build

      # Deploy to GitHub Pages
      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./dist
